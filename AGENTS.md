# his-wiki 工作规则

## 文件操作限制

- **允许新建或修改 README.md 文件**（包括 raw/README.md、各子目录 README.md 等），但必须先向用户展示完整拟写内容并获确认后再落盘
- 如发现 README.md 需要新建或更新，先起草完整内容供用户审阅；用户批准后才可执行，且变更记录写入 wiki/log.md

## 目录规则

- `raw/` 层：存放原始资料，只增不改，目录结构说明写在各子目录的 README.md 中（需用户确认后新建）
- `wiki/` 层：整理后的知识，可自由编辑非 README.md 的 .md 文件
- `schema/` 层：规范定义文件，修改前需确认

## 外部接口开发任务自动触发

当用户的需求命中以下任一信号词，**立即停下、先 Read `wiki/api/plugin-development.md`**（重点是开头的「场景识别」章节），再继续追问/写代码。目的：消除每次新会话重新解释"HIS 外部接口两条挂载线"的成本。

**事件型信号**（→ 统一事件接口，jsonParams 用 `module_code` + `function_code`）：

- 被动语态："…之后 / …时 / …完成后 / …就 / 一旦…"
- 例句："挂号成功后推送给某平台"、"收费时校验黑名单"、"病人入院自动建档"、"打标后触发上传"
- 触发主语 = HIS 业务流程，用户无感

**菜单型信号**（→ 扩展菜单，jsonParams 用 `menu_code` + `func_sign`）：

- 主动语态："在 X 界面加按钮 / 加菜单 / 加选项"、"右键多一个 XX"、"操作员点了 YY 之后…"
- 触发主语 = 用户，用户主动点击

**判断不出来**：直接反问 "这个功能是 HIS 业务流程自动触发，还是用户在界面上点按钮触发？"——**不要靠经验猜**，两条线的脚手架一样但 jsonParams 字段、配置位置完全不同，选错全错。

**进入开发态后的权威源**：

1. `wiki/api/plugin-development.md` — 工程模板、ProcessRequest 路由、version.json、部署强约束（首选）
2. `raw/外部接口开发/元his统一外部接口开发文档.md` — 原始规范结构化整理版（含 UPTools 完整方法清单 + 截图位置文字描述；原 `.docx` 作为存档保留，结构以 `wiki/` 文档为准）

**两个故意拼错的 HIS 字段**（version.json 里）——任何"自动纠正"都会让 HIS 加载失败：

- `assimbelyName`（少一个 `e`，不是 `assemblyName`）
- `flieList`（`i` 在 `e` 前，不是 `fileList`）
