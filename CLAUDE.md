# his-wiki 工作规则

## 文件操作限制

- **禁止直接新建或修改任何 README.md 文件**（包括 raw/README.md、各子目录 README.md 等）
- 如发现 README.md 需要新建或更新，只能提出建议并描述拟写内容，等用户确认后再执行

## 目录规则

- `raw/` 层：存放原始资料，只增不改，目录结构说明写在各子目录的 README.md 中（需用户确认后新建）
- `wiki/` 层：整理后的知识，可自由编辑非 README.md 的 .md 文件
- `schema/` 层：规范定义文件，修改前需确认
